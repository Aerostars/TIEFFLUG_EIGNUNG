from setuptools import find_packages, setup
setup(name="TIEFFLUG_EIGNUNG",
      version="1.0",
      description="Gust response",
      author="V.v.T.",
      author_email='aerostars@gmx.de',
      platforms=["any"],  # or more specific, e.g. "win32", "cygwin", "osx"
      license="aeroostars",
      #url="http://github.com/aerostars/TIEF_FLUG",
      packages=find_packages(),
      )
